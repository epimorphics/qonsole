import { h } from 'preact';
import { BaseComponent, BaseProps } from '../base';
interface PaginationState {
    page: number;
    limit?: number;
    total: number;
}
export interface PaginationConfig {
    enabled: boolean;
    limit?: number;
    page?: number;
    summary?: boolean;
    nextButton?: boolean;
    prevButton?: boolean;
    buttonsCount?: number;
    resetPageOnUpdate?: boolean;
    server?: {
        url?: (prevUrl: string, page: number, limit: number) => string;
        body?: (prevBody: BodyInit, page: number, limit: number) => BodyInit;
    };
}
export declare class Pagination extends BaseComponent<BaseProps & PaginationConfig, PaginationState> {
    private processor;
    static defaultProps: {
        summary: boolean;
        nextButton: boolean;
        prevButton: boolean;
        buttonsCount: number;
        limit: number;
        resetPageOnUpdate: boolean;
    };
    constructor(props: any, context: any);
    componentWillMount(): void;
    componentDidMount(): void;
    private get pages();
    private setPage;
    private setTotal;
    renderPages(): h.JSX.Element;
    renderSummary(): h.JSX.Element;
    render(): h.JSX.Element;
}
export {};
